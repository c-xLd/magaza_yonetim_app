class AppConfig {
  // Firebase kullanımını kontrol eden bayrak
  static const bool useFirebase = false;

  // Test modu çağrılarda gecikme süresi
  static const Duration testDelay = Duration(milliseconds: 500);
}
